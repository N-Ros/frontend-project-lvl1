name: linter

on:
  push:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    name: npm install
      run:
        make install
      env:
        CI: true
        - name: Run linter
        run:
          make lint