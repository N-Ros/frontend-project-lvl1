name: linter-check

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Run install
        run: |
          make install
      - name: Run Linter
        run: |
          make lint